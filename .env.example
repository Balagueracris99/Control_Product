# Ejemplo para Prisma (raíz del proyecto).
# Copia a .env y ajusta los valores.
# Debe apuntar a la MISMA base de datos que usa el backend (backend/.env).

# Formato: postgresql://USUARIO:PASSWORD@HOST:PUERTO/NOMBRE_BD
# Valores por defecto del backend: postgres, postgres, localhost, 5432, control_product
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/control_product"
